@model IEnumerable<WeltenretterDemo.Models.HeldAgressor>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Agressor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Held)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Agressor.AgressorId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Held.HeldId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HeldagressorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HeldagressorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HeldagressorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
